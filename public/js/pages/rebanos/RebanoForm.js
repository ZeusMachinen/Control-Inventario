/**
 * Página independiente de formulario de Rebaño (respaldo)
 */
const RebanoFormPage = {
  async render() {
    return MainLayout.render('<p>Usá la lista de rebaños para crear/editar desde el modal.</p>');
  },
};
